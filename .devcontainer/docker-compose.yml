version: '3.8'

services:
  app:
    container_name: app
    build: 
      context: .
      dockerfile: Dockerfile
    hostname: eggforce-sc-cep78.local
    working_dir: /workspace
    volumes:
      - ..:/workspace
    command: sleep infinity
